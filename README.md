# reclock

## Run Project

```sh
# Install dependencies with yarn!
yarn

# Run each one of this commands on a new tab (we know, we know...)

# Build your Reason code!
yarn start

# Get the node service up and running
yarn backend

# Bundle with webpack!
yarn webpack 

# Start web server to statically serve the index.html + bundle
yarn webserver
```
